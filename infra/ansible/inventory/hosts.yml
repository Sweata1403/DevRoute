all:
  children:
    jenkins:
      hosts:
        jenkins-server:
          ansible_host: "{{ lookup('env', 'JENKINS_IP') }}"

    gitlab_runners:
      hosts:
        gitlab-runner-1:
          ansible_host: "{{ lookup('env', 'GITLAB_RUNNER_IP') }}"

    monitoring:
      hosts:
        monitoring-server:
          ansible_host: "{{ lookup('env', 'MONITORING_IP') }}"
