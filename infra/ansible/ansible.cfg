[defaults]
inventory          = inventory/hosts.yml
remote_user        = ec2-user
private_key_file   = ~/.ssh/devroute-key.pem
host_key_checking  = False
retry_files_enabled = False
stdout_callback    = yaml
roles_path         = roles

[privilege_escalation]
become       = True
become_method = sudo
become_user  = root